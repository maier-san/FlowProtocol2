2025-12-12_Powersuche
Ergänze die Menüleiste im Layout um eine Suchen-Funktion "Skriptsuche" mit Eingabefeld, 
so dass man bei Eingabe eines Suchbegriffes alle *.fp2-Dateien unterhalb des 
Skriptpfades (ScriptPath aus appsettings.json) nach diesem Suchbegriff durchsucht werden. 
Die Ergebnisse sollen dann werden auf einer eigenen Ergebnisseite (Razor Page) 
als Liste dargestellt werden, wobei für jede Skriptdatei, die den Suchbegriff enthält, 
folgende Informationen angezeigt werden sollen: Name des Skriptes 
(ohne Dateiendung ".fp2"), relativer Pfad (relativ zum Skriptpfad), 
Anzahl des Auftretens innerhalb der Datei, sowie die maximal ersten drei Zeilen, 
die den Suchbegriff enthalten. Der Spriptname soll als Link anklickbar sein 
und direkt auf dir Run-Seite des jeweiligen Skriptes verweisen. 
Als Überschrift soll der Text "Skriptsuche nach <Suchbegriff> - <Anzahl> Skripte gefunden" 
stehen, wobei <Anzahl> im Fall von 0 durch "keine" ersetzt werden soll. Unter der 
Trefferliste soll wie auf der Run-Seite die Schaltfläche "Zurück zur Startseite" 
verfügbar sein.

Super! Passe die Suche jetzt bitte noch so an, dass auch der Dateipfad berücksichtigt wird, 
also dass die Skripte, deren Dateipfad den Suchbegriff enthalten, auch aufgelistet 
werden und auch vor denen in der Liste erscheinen, bei denen das nicht der Fall ist.
Die Aufruflinks für die Skripte in der Liste stimmen noch nicht. 
Verwende als Trennzeichen im Pfad %7C statt %2F und lasse die Dateiendung ".fp2" weg, 
dann sollte es passen.

----------------------------------------------------------------------------------------
2025-12-30_CamelCase-Erweiterung
Erweitere die Ersetzung der Umlaute in Zeile 39 um weitere Ersetzungen von 
diakritischen Zeichen in den jeweiligen Ausgangsbuchstaben, bzw. in eine Zeichenfolge, 
die man typischerweise als Ersatz verwendet, wenn man nur die 26 Buchstaben zur 
Verfügung hat.

----------------------------------------------------------------------------------------
2026-01-02 Sort-Erweiterung
Erweitere den Sort-Befehl um einen weiteren optionalen Parameter "ValueType=", mit dem man die Werte 
für die Sortierung wahlweise als ganze Zahl (int), Gleitkommazahl (double) oder Datum mit Uhrzeit (datetime) interpretieren kann. 
Bei ValueType=datetime soll davon ausgegangen werden, dass die Werte im Format "yyyy-MM-dd HH:mm:ss" vorliegen 
und mit einem Fehler reagiert werden, wenn dies nicht für alle Elemente der Liste der Fall ist, wobei Index und Wert 
der ersten Abweichung genannt werden sollen. Die Implementierung kann sich an der CmdDateDiff-Klasse orientieren.
Der Umgang mit Formatabweichungen für die Typen int und double soll analog sein.

----------------------------------------------------------------------------------------
2026-01-25 Chr-Ersetzung_neu
Schreibe die Ersetzung der $Chr(...)-Zeichenketten neu, und zwar so, 
dass nur die ursprünglich in input enthaltenen Auftreten ersetzt werden, 
nicht die, die durch die Ersetzungen neu entstehen.

----------------------------------------------------------------------------------------
2026-02-08_BaseKey-Input
Erweitere die Eigenschaft AssociatedInputElement in der Klasse CmdInputBaseCommand so, dass diese Eigenschaft zusätzlich in Abhängigkeit des gerade gesetzten rc.BaseKey-Wertes erfolgt, sowohl beim setzen, als auch beim abrufen. Passe auch alle aufrufenden Stellen an.

Suche eine Lösung, bei der rc.BaseKey aus dem aufrufenden Kontext übergeben wird, denn es ist keine Eigenschaft der Klassen.

----------------------------------------------------------------------------------------
2026-02-14_Tab-Icon
Ich hab dir unter FlowProtocol2\Supplements ein FavIcon in Form der Dateien favicon-32x32.png, icon-192x192.png, icon-512x512.png, apple-touch-icon-180x180.png und favicon.ico unter verschiedenen Auflösungen bereitgestellt. Binde dieses Icon so in die Anwendung ein, dass es im Browser-Tab angezeigt wird.

----------------------------------------------------------------------------------------
2026-03-07_Brotkrümelpfad
Erweitere die Seiten Select und Run um einen Brotkrümelpfad, der Ebenen von der Startseite (Start) bis zum aktuellen Ordner (bei SELECT) oder Skript (bsi Run) mit einem kleinen zentrierten Punkt als Trennzeichen auflistet.
Alle Elemente im Brotkrümelpfad sollen anklickbar sein und auf die jeweilige SELECT-, oder Run-Seite führen.
Wähle eine verhältnismäßig kleine Schriftgröße für den Brotkrümelpfad.

Siehst schon mal gut aus. Die Elemente im Brotkrümelpfad auf der Run-Seite, die Ordner darstellen, führen fälschlicherweise auch auf Run-Seiten. 
Richtig wären Links auf Select-Seiten.
Der Link auf die Start-Seite solten auf die SelectMain-Seite gehen, nicht auf /FlowPages/Select/x

In Run.cshtml, Zeile 22 wollte ich aus dem letzen Element im Brotkrümelpfad noch einen Link auf das Skript selbst machen,
so wie bei der Schaltfläche "Neue Ausführung".
Das tut soweit, allerdings werden hier die Parameter in der URL nicht zurückgesetzt. Kannst du das für mich anpassen?

----------------------------------------------------------------------------------------
2026-03-08_MultiLineInput
Implementiere die Run-Methode der schon vorbereiteten CmdMultiLineInput-Klasse aus.
Orientiere dich dabei an der CmdInputText-Klasse und baue die Funktionalität möglichst analog auf.
Der damit umzusetzende MultiLineInput-Befehl soll ähnlich wie der Input-Befehl einen Text über ein Eingabefeld abfragen und unter dem angegebenen Key in der URL speichern.
Es gibt folgende Unterschiede:
- Der MultiLineInput-Befehl soll ein mehrzeiliges Eingabefeld erzeugen, in dem falls notwendig auch ein vertikaler Scrollbalen erscheint.
- Die Länge des Eingabetextes wird nicht explizit beschränkt.
- Für die Speicherung des Textes in der URL wird der Text in geeigneter Weise komprimiert, so dass der Verbrauch an Zeichen möglichst gering gehalten wird.
Mit dem optionalen ShowLines-Parameter kann die Anzahl an sichtbaren Zeilen angegeben werden, also die Höhe des Textfeldes in Zeilen.

----------------------------------------------------------------------------------------
2026-03-14_SetCopyBlock (nicht umgesetzt)
Implementiere die Run-Methode der schon vorbereiteten CmdSetCopyBlock-Klasse aus.
Der implementierte SetCopyBlock-Befehl soll mit Hilfe der DocumentBuilder-Klasse eine neue Eigenschaft "CopyBlock" 
für den aktuellen Abschnitt (CurrentSection) auf den Wert expandedBlockName setzen.
Bei der Ausgabe soll dann hinter jeder Abschnittsüberschrift, mit der ein neuer
CopyBlock-Wert beginnt, eine Schaltfäche vorhanden sein, mit der der Inhalt des Abschnitts und
der darauffolgenden Abschnitte mit gleichem CopyBlock-Wert in die Zwischenablage übernommen werden kann.
Dabei soll auch die Formatierung des Textes mit berücksichtigt werden.
Die Schaltflächen unter den Code-Blöcken sollen dabei weggelassen werden.
Die zu implementierende Schaltfäche soll standardmäßig nicht sichtbar sein und nur dann erscheinen,
wenn man mit der Maus über den Bereich hinter der Überschrift geht. 
Sie soll ein "Kopieren in Zwischenablage"-Symbol zeigen.
Die zu kopierenden Abschnitte sollen farblich hervorgehoben werden, wenn man mit der Maus auf der Schaltfläche ist.

Das Ergebnis in der Zwischenablage ist noch nicht wie gewünscht. 
Dort werden noch HTML-Steuerzeichen ausgegeben. 
Angestrebt wird, dass das Ergebnis direkt in Outlook oder Word eingefügt werden kann 
und dort Überschriften und Aufzählungen dargestellt werden. 
Lass dich nicht von der vorhandenen Routine für das Kopieren von Codeblöcken irritieren. 
Mach die Highligt-Farbe bitte noch kräftiger.

----------------------------------------------------------------------------------------
2026-03-29_MultiLineInput - UploadFilter
Implementiere die schon vorbereitete UploadFilter-Option in der CmdMultiLineInput-Klasse aus.
In diesem Argument (konkret expandedUploadFilter) wird später ein Filter für die Auswahl von Dateien übergeben, z.B "*.*" oder "*.txt, *.log".
Die Auflistung erfolgt kommagetrennt.
Erweitere die Run-Page so, dass bei unter den über ~MultiLineInput erzeugten Eingabefelder eine Schaltfläche 
mit Beschriftung "Aus Datei einfügen" angezeigt wird, wenn die UploadFilter-Option gesetzt ist. Sonst nicht.
Orientiere dich bei Position und Aussehen der Schaltfläche an der Schaltfläche "In Zwischenablage kopieren" in _OMTextBlock.cshtml.
Der Klick auf die Schaltfläche soll clientseitig verarbeitet werden.
Es soll ein Dateiauswahldialog für die Auswahl einer lokal vorhandenen Datei geöffnet werden.
DIm Dialog soll der Filter aus der Option voreingestellt sein.
Nach Auswahl einer Datei und Bestätigung des Dialogs wird geprüft, ob es sich um eine Textdatei oder Binärdatei handelt.
Das Einlesen von Binärdateien wird mit einem Hinweis abgelehnt.
Bei einer Textdatei wird der Inhalt in das Eingabefeld übertragen, sofern er die Länge von 30000 Zeichen nicht überschreitet.
Längere Texte werden bei 30000 Zeichen abgeschnitten und es wird eine Hinweismeldung dazu ausgegeben.
Ein schon im Eingabefeld vorhandener Text wird ohne Rückfrage überschrieben.
Beachte, dass es mehrere Eingabefelder mit dieser Option geben kann. 
Orientiere dich auch hier an der "In Zwischenablage kopieren"-Option und erstelle eine zentrale Funktion im Script-Block der Run-Seite.

Erweitere das Run-Model so, dass die Notwendigkeit der clientseitigen Skript-Befehle über bool-Eigenschaften abgefraget werden kann
und dass die Skripte nur dann eingefügt werden, wenn es auch Funktionsaufrufe dafür gibt.

Die dynamische Ermittlung der Eigenschaften ist recht komplex.
Schreibe das um in eine Variante mit bool-Variablen in den Strukturen IMForm und OMDocument, 
die bei jedem Einfügen entsprechender Elemente auf true gesetzt werden und am Ende einfach abgefragt werden.
Trenne dann bitten noch die SaveFunction von der CopyFunction, weil erstere viel seltener verwendet wird.

----------------------------------------------------------------------------------------
2026-04-12_Schleifeninput
Der Zugriff auf das mit dem Eingabebefehl assoziierte Eingabeelement über AssociatedInputElements[]
von den Klassen CmdAddHelpLine, CmdAddHelpLink und CmdAddHelpText führt noch zu Problemen, 
da derselbe Befehl auch mehrmals in einer Schleife aufgerufen werden kann und mehrere Eingabeelemente erzeugt,
die sich jedoch nicht in der BaseKey-Eigenschaft unterscheiden.
Stelle den Code so um, dass jeder von CmdInputBaseCommand abgeleitete Eingabebefehl 
sein Eingabeelement in Abhängigkeit der expandedKey-Eigenschaft in AssociatedInputElements speichert
und bei jedem Run-Aufruf das zum aktuellen Key passende in einer zusätzlichen Eigenschaft
CurrentAssociatedInputElement als Verweis vorhält, so dass die Klassen CmdAddHelpLine, 
CmdAddHelpLink und CmdAddHelpText nur noch diese Eigenschaft verwenden müssen.

Ist gut, aber passt noch nicht ganz. Die Eigenschaft CurrentAssociatedInputElement 
muss bei jedem Run-Aufruf gesetzt werden, auch wenn das Eingabeelement schon 
in einem vorherigen Run-Aufruf erzeugt und im Dictionary abgelegt wurde.
